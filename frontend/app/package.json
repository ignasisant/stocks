{
  "name": "topstocks-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The app shell and its pages, rebuilt off the HTTP API. See frontend/README.md.",
  "scripts": {
    "dev": "vite",
    "build": "prettier --check src && tsc --noEmit && vitest run && vite build",
    "preview": "vite preview",
    "format": "prettier --write src index.html",
    "test": "vitest run",
    "knip": "knip"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "use-stick-to-bottom": "^1.1.6"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.1.1",
    "knip": "^6.38.0",
    "prettier": "^3.9.8",
    "typescript": "^5.9.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
